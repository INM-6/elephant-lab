Daten rueckgeben anders als JSON? Für Plots!
Messaging System im Hintergrund? ZeroMQ?
überlegen Kernel-Erweiterungen Speed => nötig? Versuchen, ohne; Wenn nicht klappt, dann ggf. mit

Port
Kommunikation schöner machen => Plot
 - ZeroMQ
	- Array mit D3 plotten
	- Pythonplot (matplotlib), dann anzeigen
        => Performance, wie funktioniert's überhaupt

Variable Inspector changes whenever a new notebook comes into focus!
Need to implement this as well!
Receiver in Python set up at the beginning and send comms from TS to this? So Python itself could react, not always sending strings of code (check this: https://jupyter-notebook.readthedocs.io/en/stable/comms.html)

Check RAM usage!

// Details of Messaging System? ZeroMQ? Is direct access to it necessary?
// Probably unnecessary due to not having to move any data, just sending execute requests via high level API

Setup a comm channel in the beginning and make Python code listen to comms, instead of sending strings of code to be executed (see also https://jupyter-notebook.readthedocs.io/en/stable/comms.html)

Variable Inspector extension changes whenever a new notebook comes into focus! Would be a great feature
Restore Jupyphant tabs and connect them to the correct notebooks

Check "compatibility" with other extensions (eg. ipympl), if all the toolbars are shown in Jupyphant plots as welll

Clean TypeScript structure
Plan a concept to enable modularity
- OOP => Manager class that registers everything, Communicator class that handles kernel communication, Tab class that shows stuff, Extension (index.ts) that instantiates the others
- Manager: Whenever new notebook is added (consoles.widgetAdded) save the widget, when new widget is in focus switch to this OR always remain bound to first active notebook and allow multiple tabs
 
Find out user requirements to create a helpful GUI
- Interactive plots? Clicking on this field shows something else? => HoloViews (Tableau as examples)
- Viziphant "megaplot" that combines lfp, event and spikes (rasterplot) into single plot
- Create neo tree with multi-select (and possibly selection by annotations and attributes), create plots according to what is selected there
- Great thing would be: Drag and Drop into CodeCell and have name of signal inserted automatically to correct position

Incrementally change plot? As an interactivity wrapper around rasterplot()

Enable dealing with live data, e.g. plotting right from Nest simulation
